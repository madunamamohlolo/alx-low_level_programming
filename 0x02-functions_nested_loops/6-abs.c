#include "main.h"
/**
 * _abs - computes the absolute value of a integer
 * @n: the int to check
 * Retutn: the absolute value of int
 */
int _abs(int n)				{						if (n >= 0)				{						return (n);			}					return (-n);			}	
